(comment) @annotation

(fn_define
	(function_header
		(type) @context
		.
		name: (_) @name)
) @item

(function_body
	(label) @name @item
)

(global_global
	(global_var) @name) @item

(global_type
	(local_var) @name) @item

(global_metadata
	(metadata_ref) @name) @item
